apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgw.fullname" . }}
  labels:
    {{- include "pgw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgw.selectorLabels" . | nindent 4 }}
