apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pgw.fullname" . }}-config
  labels:
    {{- include "pgw.labels" . | nindent 4 }}
data:
  gateway.yaml: |
    {{- .Values.gateway.config | nindent 4 }}
